@model IEnumerable<RegionalFF.Models.Ciudad>
@{
    ViewBag.Title = "Ciudades";
    ViewBag.SubTitle = "Listado";
}

<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2>@ViewBag.Title <small>@ViewBag.SubTitle</small></h2>
                <ul class="nav navbar-right panel_toolbox">
                    <li class="">
                        <a href="@Url.Action("Create", "Ciudades")" class="btn btn-default btn-xs visible-lg" data-toggle="tooltip" title="Crear Nuevo">
                            <i class="fa fa-plus"></i>
                            <strong>Crear Nuevo</strong>
                        </a>
                    </li>
                    <li class="">
                        <a href="@Url.Action("Create", "Ciudades")" class="btn btn-default btn-xs hidden-lg" data-toggle="tooltip" title="Crear Nuevo">
                            <i class="fa fa-plus"></i>
                        </a>
                    </li>
                </ul>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">
                <div class="table-responsive">
                    <table id="datatable" class="table table-striped table-condensed table-hover" cellspacing="0" width="100%">
                        <thead>
                            <tr class="">
                                <th>
                                    <i class="fa fa-cog"></i>
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Nombre)
                                </th>
                            </tr>
                        </thead>


                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="col-lg-2 btn-group">
                                        <div class="pull-right">
                                            <a class="btn btn-xs btn-warning" href="@Url.Action("Edit", "Ciudades", new { id = item.Id })" data-toggle="tooltip" data-placement="top" title="Editar"><i class="fa fa-edit"></i> </a>

                                            <a class="btn btn-xs btn-primary" href="@Url.Action("Details", "Ciudades", new { id = item.Id })" data-toggle="tooltip" data-placement="top" title="Ver Detalles"><i class="fa fa-list-alt"></i> </a>

                                            <a class="btn btn-xs btn-danger" href="@Url.Action("Delete", "Ciudades", new { id = item.Id })" data-toggle="tooltip" data-placement="top" title="Borrar"><i class="fa fa-trash-o"></i> </a>
                                        </div>
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Nombre)
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/datatables")
}
