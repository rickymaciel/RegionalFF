@model IEnumerable<RegionalFF.Models.Rol>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Administrar Roles";
    ViewBag.SubTitle = "Lista de Roles";
}




<div class="">
    <div class="row">
        <div class="col-md-12 col-sm-12 col-xs-12">
            <div class="x_panel">
                <div class="x_title">
                    <h2>@ViewBag.Title <small>@ViewBag.SubTitle</small></h2>
                    <ul class="nav navbar-right panel_toolbox">
                        <li>
                            <a href="@Url.Action("Index", "Admin")" class="">
                                <i class="fa fa-reply"></i>
                                Volver a Usuarios
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("NuevoRol", "Admin")" class="">
                                <i class="fa fa-plus-circle"></i>
                                Nuevo Rol
                            </a>
                        </li>
                    </ul>
                    <div class="clearfix"></div>
                </div>
                <div class="x_content">
                    <div class="">

                        @*@using (Html.BeginForm("Index", "Admin", FormMethod.Get))
                            {
                                <div class="col-md-7">
                                    <div class="input-group">
                                        @Html.TextBox("searchStringUserNameOrEmail", ViewBag.CurrentFilter as string, "", new { @class = "form-control", @autocomplete = "off", @placeholder = "Buscar por nombre de Usuario o Email ..." })
                                        <span class="input-group-btn">
                                            <button type="submit" class="btn btn-primary"><i class="fa fa-search"></i> Buscar</button>
                                        </span>
                                    </div>
                                </div>
                            }*@


                        <table id="datatable" class="table table-striped table-hover table-condensed dt-responsive nowrap" cellspacing="0" width="100%">
                            <thead>
                                <tr>
                                    <th>
                                        <i class="fa fa-cog text-center"></i>
                                    </th>
                                    <th>
                                        Roles
                                    </th>
                                </tr>
                            </thead>


                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td class="col-lg-2" align="center">
                                            @if (item.RoleName.ToLower() != "Administrator")
                                            {
                                                <a class="btn btn-warning" href="@Url.Action("EditarRol", "Admin", new { roleName = item.RoleName })" data-toggle="tooltip" data-placement="top" title="Editar"><em class="fa fa-pencil"></em> </a>
                                                <a class="btn btn-danger" onclick="return confirm('¿Seguro que desea eliminar este usuario?');" href="@Url.Action("DeleteUserRole", "Admin", new { UserName = item.RoleName })" data-toggle="tooltip" data-placement="top" title="Borrar"><em class="fa fa-trash"></em></a>
                                            }
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.RoleName)
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>
