@model RegionalFF.Models.ForgotPasswordViewModel
@{
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
    ViewBag.Title = "Restablecer";
    ViewBag.Denominacion = "RegionalFF";
}


<section class="login_content">
    <img src="~/Content/img/RegionalFFlogin.png" class="image" width="100" title="@ViewBag.Denominacion" alt="@ViewBag.Denominacion" />


    @using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "", role = "form" }))
    {
        @Html.AntiForgeryToken()
        <h1>
            @*<i class="fa fa-exchange"></i>*@  @ViewBag.Title
        </h1>
        @*<div class="col-md-12">
            <div class="bs-callout bs-callout-info" id="callout-pagination-label">
                <p class="text-justify">
                    Introduzca su dirección de correo electrónico y le enviaremos un enlace para restablecer su contraseña.
                </p>
            </div>
        </div>*@
        <div class="form-group">
            <div class="col-lg-12 col-md-12 col-xs-12">
                @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger pull-left" })
                @Html.TextBoxFor(m => m.Email, new { @placeholder = "Ingrese su correo electrónico", @class = "form-control" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-lg-12 col-md-12 col-xs-12">
                <button type="submit" class="btn btn-primary btn-block"><i class="fa fa-send"></i> Enviar email de restablecimiento</button>
                <a class="reset_pass" href="@Url.Action("Login", "Account")">Iniciar Sesión</a>
            </div>
        </div>

        <br />
        <div class="clearfix"></div>

        <div class="separator">

            <div class="clearfix"></div>
            <br />

            <div>
                <p>&copy; @DateTime.Now.Year - @ViewBag.Denominacion</p>
            </div>
        </div>
    }
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}