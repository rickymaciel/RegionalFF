@model IEnumerable<RegionalFF.Models.Pais>

@{
    ViewBag.Title = "Paises";
    ViewBag.SubTitle = "Listado";
}

<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2>@ViewBag.Title <small>@ViewBag.SubTitle</small></h2>
                <ul class="nav navbar-right panel_toolbox">
                    <li class="">
                        <a href="@Url.Action("Create", "Paises")" class="btn btn-default btn-xs visible-lg" data-toggle="tooltip" title="Crear Nuevo">
                            <i class="fa fa-plus"></i>
                            <strong>Crear Nuevo</strong>
                        </a>
                    </li>
                    <li class="">
                        <a href="@Url.Action("Create", "Paises")" class="btn btn-default btn-xs hidden-lg" data-toggle="tooltip" title="Crear Nuevo">
                            <i class="fa fa-plus"></i>
                        </a>
                    </li>
                </ul>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">
                @*<p class="text-muted font-13 m-b-30">
                    Administrar el Menú: <code></code>
                </p>*@
                <div class="table-responsive">
                    <table id="datatable" class="table table-striped table-condensed table-hover" cellspacing="0" width="100%">
                        <thead>
                            <tr>
                                <th></th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Nombre)
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="col-lg-2" align="center">
                                        <a class="btn btn-sm btn-warning" href="@Url.Action("Edit", "Paises", new { id = item.Id })" data-toggle="tooltip" data-placement="top" title="Editar"><em class="fa fa-pencil"></em> </a>

                                        <a class="btn btn-sm btn-primary" href="@Url.Action("Details", "Paises", new { id = item.Id })" data-toggle="tooltip" data-placement="top" title="Ver Detalles"><em class="fa fa-eye"></em> </a>

                                        <a class="btn btn-sm btn-danger" href="@Url.Action("Delete", "Paises", new { id = item.Id })" data-toggle="tooltip" data-placement="top" title="Borrar"><em class="fa fa-trash"></em> </a>
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Nombre)
                                    </td>
                                </tr>
                            }
                        </tbody>

                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
